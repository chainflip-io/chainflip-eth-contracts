testMode: "property"
#codeSize Increased codeSize because Echidna fails when deploying all the contracts via DeployerEchidna.sol
codeSize: 0x60000

cryticArgs: # If we add more dependencies they will need to be added as symlinks here!
  [
    "--solc-remaps",
    "../../node_modules/@openzeppelin/contracts/=node_modules/../../node_modules/@openzeppelin/contracts/",
  ]