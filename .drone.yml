kind: pipeline
name: chainflip-eth-contracts
type: docker

clone:
  disable: true
steps:
  - name: test
    environment:
      PYTHONPATH: /tmp/chainflip-eth-contracts
    image: ghcr.io/chainflip-io/eth-brownie:latest
    commands:
      - cd /tmp
      - git clone https://github.com/chainflip-io/chainflip-eth-contracts.git
      - cd chainflip-eth-contracts
      - brownie test
trigger:
  event:
    - push
image_pull_secrets:
  - dockerconfigjson
