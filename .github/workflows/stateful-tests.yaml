name: Stateful Tests
on: 
  push:
    branches:
      - master

jobs:
  run-tests:
    name: Stateful Tests
    uses: chainflip-io/chainflip-eth-contracts/.github/workflows/.test.yaml@master
    with:
      stateful: true
      timeout_minutes: 3600
