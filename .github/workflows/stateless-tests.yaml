name: Stateless Tests
on: push

jobs:
  lint:
    name: Linting
    uses: chainflip-io/chainflip-eth-contracts/.github/workflows/.lint.yaml@master

  # stateless-tests:
  #   name: Stateless Tests
  #   needs: lint
  #   uses: chainflip-io/chainflip-eth-contracts/.github/workflows/.test.yaml@feat/contract-updates
  #   with:
  #     stateful: false

  stateful-tests:
    name: Stateful Tests
    needs: lint
    uses: chainflip-io/chainflip-eth-contracts/.github/workflows/.test.yaml@feat/contract-updates
    with:
      stateful: true