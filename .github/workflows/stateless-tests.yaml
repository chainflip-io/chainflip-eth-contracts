name: Stateless Tests
on: push

jobs:
  lint:
    name: Linting
    uses: chainflip-io/chainflip-eth-contracts/.github/workflows/.lint.yaml@master

  stateless-tests:
    name: Stateless Tests
    needs: lint
    uses: chainflip-io/chainflip-eth-contracts/.github/workflows/.test.yaml@fix/poetry
    with:
      stateful: false

